{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://api.agmip.org/schemas/annotated-data/2020-09/mapping-info.json",
    "title": "General Mapping Metadata",
    "description": "Metadata about this specific mapping.",
    "type": "object",
    "properties": {
        "mapping_author": {
            "type": "string",
            "description": "Indicator of the mapping author, SHOULD be an email address.",
            "$comment": "This field needs to be discuessed and expanded to be better for researchers"
        },
        "source_url": {
            "type": "string",
            "description": "The base URL (DOI) for the entire dataset."
        }
    },
    "required": ["source_url", "mapping_author"]
}